'use client';

import { useState } from 'react';

const UploadButtonFile = () => {
  const [isOpen, setIsOpen] = useState<boolean>(false);

  return <div>UploadButtonFile</div>;
};

export default UploadButtonFile;
