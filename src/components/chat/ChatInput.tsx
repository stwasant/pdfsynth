import React from 'react'
interface ChatInputProps {
    isDisable?: boolean;
}
const ChatInput: React.FC<ChatInputProps> = ({ isDisable }) => {
  return (
    <div>ChatInput</div>
  )
}

export default ChatInput